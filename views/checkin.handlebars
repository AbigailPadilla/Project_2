<div class="checkin">
  <div class="container">
      
      <div class="row limited-width">
        <div class="col-lg-12">
          <div class="jumbotron">
            <h1 class="text-center">
              <h1>DR READY</h1>
            <hr>
            <h2 class="text-center">CHECK IN FOR YOUR APPOINMENT</h2>
            <br>
          </div>
          <!-- Check in Page -->
          <div class="card">
            <div class="card-header">
              CHECK INTO YOUR APPOINMENT
            </div>
            <div class="card-body">
              <form role="form">
                <div class="form-group">
                  <label for="patient-name">Name</label>
                  <input type="text" class="form-control" id="patient-name">
                </div>
                
                <div class="form-group">
                  <label for="patient-email">Email</label>
                  <input type="text" class="form-control" id="patient-email">
                </div>
                
                <button type="submit" class="btn btn-primary submit">Submit</button>
              </form>
            </div>
          </div>
        </div>
      </div>


      <footer class="footer">
        <div class="container">
          
      </footer>

  </div>
</div>



<!-- BELOW CODE IS CRITICAL. IT HANDLES HOW FORM DATA IS SENT TO OUR SERVER -->
<script type="text/javascript">
  

  $(".submit").on("click", function(event) {
    event.preventDefault();

    // Here we grab the form elements
    var APPOINMENT= {
      patientName: $("#patient-name").val().trim(),
      
      patientEmail: $("#patient-email").val().trim(),
      
    };

    console.log(APPOINMENT);

    // This line is the magic. It"s very similar to the standard ajax function we used.
    // Essentially we give it a URL, we give it the object we want to send, then we have a "callback".
    // The callback is the response of the server. In our case, we set up code in api-routes that "returns" true or false
    // depending on if a tables is available or not.

    $.post("/api/appointments", APPOINMENT,
      function(data) {

        
        if (data) {
          alert("Yay! You are officially CHECK-IN!");
        }

        
        else {
          alert("Sorry you are not check in");
        }

        // Clear the form when submitting
        $("#checkin-name").val("");
        
        $("#checkin-email").val("");
        
       window,location="/"
      });

  });

</script>
